// Objeto Math

let resultado;

resultado = Math.PI;
// resultado = Math.round(2.5);
// resultado = Math.ceil(2.5); // ceil siempre rendondea hacia arriba
// resultado = Math.floor(2.9); // floor siempre rendondea hacia abajo
// resultado = Math.sqrt(144); // sqrt saca la raiz cuadrada
// resultado = Math.abs(-200); // convierte el valor negativo a positivo
// resultado = Math.min( 3, 5, 1, 8, 2, 10); // encuentra el valor mas pequeño
// resultado = Math.max( 3, 5, 1, 8, 2, 10); // encuentra el valor mas alto
// resultado = Math.random(); // genera un numero aleatorio < 1
// resultado = Math.floor(Math.random() * 100); // genera un numero aleatorio

console.log(resultado);